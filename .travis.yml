language: node_js
node_js:
  - 8
  - 9
  - node
after_script: NODE_ENV=test nyc cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage
